{% extends 'back_base.html.twig' %}

{% block title %}Edit Categories{% endblock %}

{% block body %}
   
    {{ include('back/category/_form.html.twig', {'button_label': 'Update'}) }}
   
    <div class="flex justify-start">
        <a href="{{ path('admin_app_category_index') }}" class="text-indigo-600 hover:text-indigo-900">
            <button type="submit" class="ml-3 inline-flex justify-center rounded-md border border-transparent bg-indigo-600 py-2 px-4 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">Back</button>
        </a>
    </div>
{% endblock %}
